# Shared NON-SECRET bot settings.
# Keep keys/wallet addresses in .env only.
#
# Usage:
# 1) cp config/bot.public.env.example config/bot.public.env
# 2) Edit this file and share it safely.
# 3) Keep .env with secrets only (private key, proxy wallet, telegram token, etc.)
#
# Optional: set BOT_PUBLIC_CONFIG_FILE to use another path.
# Example:
#   BOT_PUBLIC_CONFIG_FILE=./config/settings.env

# Optional wallet selector for multi-wallet runs (non-secret).
# Per-wallet keys still live in .env:
#   POLYMARKET_PRIVATE_KEY_<ID>
#   POLYMARKET_PROXY_ADDRESS_<ID>
# Optional per-wallet signature type in .env:
#   SIGNATURE_TYPE_<ID>=1
BOT_WALLET_ID=

# =============================================================================
# MODE
# =============================================================================
PAPER_TRADING=false
PAPER_BALANCE_USD=1000

# =============================================================================
# RISK / SIZING
# =============================================================================
ADAPTIVE_RISK_ENABLED=false
ORDER_SIZE_USD=5
MAX_POSITION_USD=15
MAX_TOTAL_EXPOSURE_USD=9999999
DAILY_LOSS_LIMIT_USD=9999999
MIN_BALANCE_USD=5
MAX_ACTIVE_ORDERS=999
ORDER_TIMEOUT_SECONDS=300

# =============================================================================
# BOT BEHAVIOR
# =============================================================================
SCAN_INTERVAL_SECONDS=2
MARKET_REFRESH_SECONDS=300
BALANCE_REFRESH_SECONDS=60
ENABLE_WEBSOCKET_FEED=false
LOG_LEVEL=INFO
RETRY_MAX_ATTEMPTS=3
RETRY_BASE_DELAY_SECONDS=0.5
RETRY_MAX_DELAY_SECONDS=5
AUTO_ALLOWANCE_REFRESH_ENABLED=true
ALLOWANCE_REFRESH_SECONDS=900
ENABLE_STRATEGY_ANALYTICS=false
# Leave BOT_STATE_DB unset to auto-use per-wallet DB:
# - bot_state_<wallet_id>.sqlite when BOT_WALLET_ID is set
# - bot_state.sqlite otherwise
# BOT_STATE_DB=./data/bot_state.sqlite

# =============================================================================
# STRATEGIES (Plan B trio only)
# =============================================================================
DISABLED_STRATEGIES=spread,arbitrage,favorite_longshot,cross_platform_arbitrage,cross_asset,orderbook_imbalance,stink_bid,late_money,vpin,sentiment
ENABLE_BTC_5MIN=false

# =============================================================================
# MARKET FILTERS
# =============================================================================
ONLY_CRYPTO_MARKETS=false
MIN_PRICE_CENTS=5
MAX_PRICE_CENTS=95
MIN_VOLUME_USD=1000

# =============================================================================
# TERMINAL CONVERGENCE
# =============================================================================
TERMINAL_CONVERGENCE_WINDOW_SECONDS=60
TERMINAL_MIN_EDGE_CENTS=3

# =============================================================================
# COMBINATORIAL ARB
# =============================================================================
COMBO_MIN_EDGE_CENTS=3
COMBO_COOLDOWN=300

# =============================================================================
# WALLET COPY
# =============================================================================
TRACKED_WALLETS=
WALLET_COPY_USE_LEADERBOARD=true
WALLET_COPY_LEADERBOARD_TOP_N=4
WALLET_COPY_LEADERBOARD_CATEGORY=CRYPTO
WALLET_COPY_LEADERBOARD_PERIOD=MONTH
WALLET_COPY_SIZE_USD=4
WALLET_COPY_SIZE_MULTIPLIER=1.0
WALLET_COPY_MAX_DELAY_SECONDS=80
WALLET_COPY_MIN_TRADE_USD=10
WALLET_COPY_CRYPTO_ONLY=true
WALLET_COPY_COOLDOWN_SECONDS=60
